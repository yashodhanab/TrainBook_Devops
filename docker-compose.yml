# version: '3.8'
# services:
#   backend:
#     build: ./traindevback
#     image: trainbook-backend:${IMAGE_TAG:-latest}
#     container_name: trainbook_backend
#     ports:
#       - "5000:5000"
#     environment:
#       - NODE_ENV=development
#       - MONGODB_URI=mongodb://mongo:27017/auth_db
#     depends_on:
#       - mongo
#     restart: unless-stopped

#   frontend:
#     build: ./traindev
#     image: trainbook-frontend:${IMAGE_TAG:-latest}
#     container_name: trainbook_frontend
#     ports:
#       - "5173:5173"
#     depends_on:
#       - backend
#     restart: unless-stopped

#   mongo:
#     image: mongo:6
#     container_name: mongo-db
#     ports:
#       - "27017:27017"
#     environment:
#       - MONGO_INITDB_DATABASE=auth_db
#     volumes:
#       - mongo_data:/data/db
#     restart: unless-stopped

# volumes:
#   mongo_data:

version: '3.8'
services:
  backend:
    # Use the image pushed by Jenkins, not a local build
    image: ${DOCKER_USERNAME}/${BACKEND_IMAGE}:${IMAGE_TAG}
    container_name: trainbook_backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - MONGODB_URI=mongodb://mongo:27017/auth_db
    depends_on:
      - mongo
    restart: unless-stopped

  frontend:
    # Use the image pushed by Jenkins
    image: ${DOCKER_USERNAME}/${FRONTEND_IMAGE}:${IMAGE_TAG}
    container_name: trainbook_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: unless-stopped

  mongo:
    image: mongo:6
    container_name: mongo-db
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=auth_db
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
